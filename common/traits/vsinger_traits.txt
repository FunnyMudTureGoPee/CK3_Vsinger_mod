vsinger_miku = {
    is_good = yes
    flag = vsinger_nation_maid
    requires_trait = vsinger_base
    icon = "cm_hand.dds"

    character_modifier = {
        diplomacy = 2
        intrigue = 1
        learning = 2
        general_opinion = 10
        monthly_prestige = 0.2
    }

    desc = {
        first_valid = {
            triggered_desc = {
                trigger = { NOT = { exists = this } }
                desc = trait_vsinger_miku_desc
            }
            desc = trait_vsinger_miku_character_desc
        }
    }

    opposites = { vsinger_luka }
    group = special
    genetic = no
    random_creation = 0
}

vsinger_luka = {
    is_good = yes
    flag = vsinger_nation_maid
    requires_trait = vsinger_base
    icon = "cm_hand.dds"

    character_modifier = {
        martial = 2
        prowess = 4
        diplomacy = 1
        general_opinion = 10
        monthly_prestige = 0.2
    }

    desc = {
        first_valid = {
            triggered_desc = {
                trigger = { NOT = { exists = this } }
                desc = trait_vsinger_luka_desc
            }
            desc = trait_vsinger_luka_character_desc
        }
    }

    opposites = { vsinger_miku }
    group = special
    genetic = no
    random_creation = 0
}

vsinger_miku_v1 = {
    is_good = yes
    flag = vsinger_nation_maid
    requires_trait = vsinger_base
    icon = "cm_hand.dds"

    character_modifier = {
        diplomacy = 2
        intrigue = 1
        learning = 2
        general_opinion = 10
        monthly_prestige = 0.2
    }

    desc = {
        first_valid = {
            triggered_desc = {
                trigger = { NOT = { exists = this } }
                desc = trait_vsinger_miku_v1_desc
            }
            desc = trait_vsinger_miku_v1_character_desc
        }
    }

    opposites = { vsinger_luka }
    group = special
    genetic = no
    random_creation = 0
}

vsinger_miku_v2 = {
    is_good = yes
    flag = vsinger_nation_maid
    requires_trait = vsinger_base
    icon = "cm_hand.dds"

    character_modifier = {
        diplomacy = 2
        intrigue = 1
        learning = 2
        general_opinion = 10
        monthly_prestige = 0.2
    }

    desc = {
        first_valid = {
            triggered_desc = {
                trigger = { NOT = { exists = this } }
                desc = trait_vsinger_miku_v2_desc
            }
            desc = trait_vsinger_miku_v2_character_desc
        }
    }

    opposites = { vsinger_luka }
    group = special
    genetic = no
    random_creation = 0
}

# --- Auto-generated by script: Please fill in details ---

vsinger_luka_test = {
    is_good = yes
    flag = vsinger_nation_maid # TODO: You might want to change this
    requires_trait = vsinger_base
    icon = "cm_hand.dds" # TODO: Change icon if needed

    character_modifier = {
        # TODO: Add character modifiers here
    }

    desc = {
        first_valid = {
            triggered_desc = {
                trigger = { NOT = { exists = this } }
                desc = trait_vsinger_luka_test_desc
            }
            desc = trait_vsinger_luka_test_character_desc
        }
    }

    # opposites = { ... }
    group = special
    genetic = no
    random_creation = 0
}

vsinger_miku_test = {
    is_good = yes
    flag = vsinger_nation_maid # TODO: You might want to change this
    requires_trait = vsinger_base
    icon = "cm_hand.dds" # TODO: Change icon if needed

    character_modifier = {
        # TODO: Add character modifiers here
    }

    desc = {
        first_valid = {
            triggered_desc = {
                trigger = { NOT = { exists = this } }
                desc = trait_vsinger_miku_test_desc
            }
            desc = trait_vsinger_miku_test_character_desc
        }
    }

    # opposites = { ... }
    group = special
    genetic = no
    random_creation = 0
}
